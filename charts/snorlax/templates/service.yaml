apiVersion: v1
kind: Service
metadata:
  name: {{ include "snorlax.fullname" . }}
  labels:
    {{- include "snorlax.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "snorlax.selectorLabels" . | nindent 4 }}
